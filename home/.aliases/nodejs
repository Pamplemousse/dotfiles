alias http-server=" \
  docker run \
    --name http-server \
    --rm -it \
    -e USER_ID=$(id -u) \
    -e GROUP_ID=$(id -g) \
    -p 8080:8080 \
    -v $(pwd):/data \
    -w /data \
    pamplemousse/nodejs \
    http-server"

function node() {
  docker run \
    --rm -it \
    -e USER_ID=$(id -u) \
    -e GROUP_ID=$(id -g) \
    -v $(pwd):/data \
    -w /data \
    pamplemousse/nodejs \
    "node $@"
}

function npm() {
  docker run \
    --rm -it \
    -e USER_ID=$(id -u) \
    -e GROUP_ID=$(id -g) \
    -v $(pwd):/data \
    -w /data \
    pamplemousse/nodejs \
    "npm $@"
}

alias nsp=" \
  docker run \
    --rm -it \
    -e USER_ID=$(id -u) \
    -e GROUP_ID=$(id -g) \
    -v $(pwd):/data \
    -w /data \
    pamplemousse/nodejs \
    nsp"

alias standard=" \
  docker run \
    --name http-server \
    --rm \
    -e USER_ID=$(id -u) \
    -e GROUP_ID=$(id -g) \
    -v $(pwd):/data \
    -w /data \
    pamplemousse/nodejs \
    standard"
